# Prepare Push

Prepare an unsigned XDR transaction to move a credential between vaults.

## POST `/tx/prepare/push`

See [Vault Push](../vault/push.md) for complete documentation.

This endpoint prepares the transaction that can be signed and submitted via `/vault/push`.
