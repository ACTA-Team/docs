# Schema

Outline of ACTA credential-related payloads, responses, and core entities based on the API implementation.

- Basic: primitives and identifiers used across endpoints
- Entities: credential and verification objects
- Payloads: request bodies per endpoint
- Responses: response bodies per endpoint
- Errors: standardized error codes and validation messages

Sources referenced inline using `file_path:line_number`.